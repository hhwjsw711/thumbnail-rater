/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "necessary-cod-361.convex.cloud",
            },
        ],
    },
};

export default nextConfig;
